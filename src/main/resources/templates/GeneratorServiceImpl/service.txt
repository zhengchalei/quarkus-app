package {data.modulePath}.service;

import {data.entityPath}.{data.entityName};
import io.quarkus.panache.common.Page;

import javax.validation.Valid;
import javax.validation.constraints.NotNull;
import java.util.List;

/**
 * @author <a href="mailto:stone981023@gmail.com">zhengchalei</a>
 **/
public interface {data.entityName}Service {

    List<{data.entityName}> findPage(@NotNull Page page,@NotNull {data.entityName} {data.lowerCaseEntityName});

    long findCount(@NotNull Page page, @NotNull {data.entityName} {data.lowerCaseEntityName});

    List<{data.entityName}> findList(@NotNull {data.entityName} {data.lowerCaseEntityName});

    {#if data.tree}
    List<{data.entityName}> tree();
    {/if}

    {data.entityName} findById(@NotNull Long id);

    void save({data.entityName} {data.lowerCaseEntityName});

    void update(@NotNull Long id, {data.entityName} {data.lowerCaseEntityName});

    boolean delete(@NotNull Long id);
}
