package {data.modulePath}.service;

import {data.entityPath}.{data.entityName};
import io.quarkus.panache.common.Page;

import javax.validation.Valid;
import javax.validation.constraints.NotNull;
import java.util.List;

/**
 * @author <a href="mailto:stone981023@gmail.com">zhengchalei</a>
 **/
public interface {data.entityName}Service {

    List<{data.entityName}> findPage(Page page, {data.entityName} {data.lowerCaseEntityName});

    long findCount(Page page, {data.entityName} {data.lowerCaseEntityName});

    List<{data.entityName}> findAll({data.entityName} {data.lowerCaseEntityName});

    {#if data.tree}
    List<{data.entityName}> tree();
    {/if}

    {data.entityName} findById(@Valid @NotNull Long id);

    void save(@Valid {data.entityName} {data.lowerCaseEntityName});

    void update(@Valid {data.entityName} {data.lowerCaseEntityName});

    boolean delete(@Valid @NotNull Long id);
}
